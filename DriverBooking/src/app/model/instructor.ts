import { Title } from '@angular/platform-browser';

export interface instructor {
    Lid?: number,
    name?: string,
    lastName?: string,
    Phonenumber?: number,
    Email?: string,
    picture_path?: string
};

export class bookingup
{
        //bid?: number,
        bookingDate: String;
        startTime: String;
        endTime: String;
};